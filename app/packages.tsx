import { Box } from '@chakra-ui/react';
import React from 'react';

const PackagesPage = () => {
    return (
        <Box>
            packages page
        </Box>
    );
};

export default PackagesPage;